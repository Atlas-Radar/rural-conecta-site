---
import { availabilityRegions, site } from "../../data/site";

const routeSteps = ["Região", "Ponto", "Resultado"] as const;
---

<section
  id="disponibilidade"
  class="availability-modal-host"
  data-availability-root
  data-whatsapp-digits={site.whatsapp.digits}
>
  <dialog
    class="availability-modal"
    aria-modal="true"
    aria-labelledby="availability-modal-title"
    aria-describedby="availability-modal-description"
    data-availability-modal
    tabindex="-1"
  >
    <div class="availability-modal__surface">
      <header class="availability-modal__header">
        <div class="availability-modal__title-group">
          <span class="availability-modal__target" aria-hidden="true">
            <svg viewBox="0 0 48 48" focusable="false">
              <path d="M24 5v8"></path>
              <path d="M24 35v8"></path>
              <path d="M5 24h8"></path>
              <path d="M35 24h8"></path>
              <circle cx="24" cy="24" r="15"></circle>
              <circle cx="24" cy="24" r="8"></circle>
              <circle cx="24" cy="24" r="2.8"></circle>
            </svg>
          </span>
          <div>
            <h2 id="availability-modal-title">
              Pré-análise de disponibilidade
            </h2>
            <p id="availability-modal-description">
              Escolha a região e marque o ponto.
            </p>
          </div>
        </div>

        <button
          class="availability-modal__close"
          type="button"
          aria-label="Fechar pré-análise"
          data-availability-modal-close
        >
          <svg aria-hidden="true" focusable="false" viewBox="0 0 24 24">
            <path d="m6 6 12 12"></path>
            <path d="M18 6 6 18"></path>
          </svg>
        </button>
      </header>

      <ol class="availability-modal__stepper" aria-label="Etapas da consulta">
        {
          routeSteps.map((step, index) => (
            <li class={index === 0 ? "is-active" : undefined}>
              <span>{index + 1}</span>
              <strong>{step}</strong>
            </li>
          ))
        }
      </ol>

      <div class="availability-modal__body">
        <div class="availability-modal__controls">
          <div class="availability-region-card">
            <div class="availability-region-card__copy">
              <svg aria-hidden="true" focusable="false" viewBox="0 0 24 24">
                <path d="M12 21s6-5.4 6-10a6 6 0 1 0-12 0c0 4.6 6 10 6 10Z"
                ></path>
                <circle cx="12" cy="11" r="2"></circle>
              </svg>
              <div>
                <h3>Região ou localidade</h3>
              </div>
            </div>

            <label class="field-label" for="availability-region">
              Possível região/localidade
            </label>
            <select
              id="availability-region"
              class="field-control"
              data-availability-region
            >
              {
                availabilityRegions.map((region) => (
                  <option value={region} selected={region === "Novo Barreiro"}>
                    {region}
                  </option>
                ))
              }
            </select>
            <p
              class="availability-modal__microcopy"
              data-availability-region-status
            >
              Regiões carregam quando a pré-análise é aberta.
            </p>
          </div>

          <button
            class="availability-realtime"
            type="button"
            data-availability-realtime-location
          >
            <span class="availability-realtime__icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" focusable="false">
                <path d="M12 3v3"></path>
                <path d="M12 18v3"></path>
                <path d="M3 12h3"></path>
                <path d="M18 12h3"></path>
                <circle cx="12" cy="12" r="5.8"></circle>
                <circle cx="12" cy="12" r="1.7"></circle>
              </svg>
            </span>
            <span data-availability-realtime-label> Usar GPS agora </span>
            <small>GPS</small>
          </button>

          <div class="availability-modal__divider" aria-hidden="true">
            <span></span>
            <strong>outras opções</strong>
            <span></span>
          </div>

          <div
            class="availability-actions"
            aria-label="Outras formas de informar"
          >
            <button
              class="availability-action"
              type="button"
              data-availability-action="search"
            >
              <span class="availability-action__icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" focusable="false">
                  <circle cx="10.5" cy="10.5" r="5.4"></circle>
                  <path d="m15 15 4.2 4.2"></path>
                </svg>
              </span>
              <span>
                <strong>Busca ou mapa</strong>
                <small>Pesquisar, tocar no mapa ou arrastar marcador.</small>
              </span>
              <svg
                class="availability-action__arrow"
                aria-hidden="true"
                viewBox="0 0 24 24"
              >
                <path d="m9 6 6 6-6 6"></path>
              </svg>
            </button>

            <button
              class="availability-action"
              type="button"
              data-availability-action="map"
            >
              <span class="availability-action__icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" focusable="false">
                  <path d="m4 6 5-2 6 2 5-2v14l-5 2-6-2-5 2Z"></path>
                  <path d="M9 4v14"></path>
                  <path d="M15 6v14"></path>
                </svg>
              </span>
              <span>
                <strong>Escolher no mapa</strong>
                <small>Abrir mapa para marcar o ponto.</small>
              </span>
              <svg
                class="availability-action__arrow"
                aria-hidden="true"
                viewBox="0 0 24 24"
              >
                <path d="m9 6 6 6-6 6"></path>
              </svg>
            </button>

            <button
              class="availability-action"
              type="button"
              data-availability-action="coordinates"
            >
              <span class="availability-action__icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" focusable="false">
                  <path d="M12 4v3"></path>
                  <path d="M12 17v3"></path>
                  <path d="M4 12h3"></path>
                  <path d="M17 12h3"></path>
                  <circle cx="12" cy="12" r="4"></circle>
                </svg>
              </span>
              <span>
                <strong>Informar coordenadas</strong>
                <small>Digite o ponto diretamente.</small>
              </span>
              <svg
                class="availability-action__arrow"
                aria-hidden="true"
                viewBox="0 0 24 24"
              >
                <path d="m9 6 6 6-6 6"></path>
              </svg>
            </button>
          </div>

          <div
            class="availability-coordinate-card"
            data-availability-coordinate-panel
            hidden
          >
            <div class="availability-coordinate-card__heading">
              <label class="field-label" for="availability-coordinates">
                Coordenadas do ponto
              </label>
              <span>Conversão automática</span>
            </div>
            <input
              id="availability-coordinates"
              class="field-control"
              type="text"
              inputmode="text"
              placeholder="-16.448694, -46.906306 ou 16°26'55&quot;S, 46°54'22&quot;W"
              autocomplete="off"
              data-availability-coordinates
            />
            <p class="field-hint">Aceita DD, DMS ou lat,lng.</p>
          </div>

          <p class="availability-modal__helper" data-availability-helper>
            Use o GPS ou abra uma opção para informar o ponto.
          </p>

          <button
            class="availability-submit"
            type="button"
            data-availability-submit
            disabled
          >
            <span>Consultar pré-análise</span>
            <svg aria-hidden="true" focusable="false" viewBox="0 0 24 24">
              <path d="M5 12h13"></path>
              <path d="m13 6 6 6-6 6"></path>
            </svg>
          </button>

          <p class="privacy-note availability-modal__privacy">
            Sem cadastro ou dados pessoais.
          </p>
          <div
            class="availability-result"
            data-availability-result
            aria-live="polite"
            hidden
          >
            <p class="availability-result__eyebrow">Resultado da pré-análise</p>
            <h3 data-availability-result-title>Pré-análise</h3>
            <p data-availability-result-message>
              A resposta aparecerá aqui depois da consulta.
            </p>
            <div class="availability-result__actions">
              <a
                class="availability-result__whatsapp"
                href={`https://wa.me/${site.whatsapp.digits}`}
                target="_blank"
                rel="noopener noreferrer"
                data-availability-whatsapp
              >
                Continuar pelo WhatsApp
              </a>
              <button
                class="availability-result__edit"
                type="button"
                data-availability-edit
              >
                Editar coordenadas
              </button>
            </div>
          </div>
        </div>

        <aside
          class="availability-modal__map"
          aria-label="Mapa para escolher o ponto da propriedade"
          data-availability-map-panel
          hidden
        >
          <div class="availability-map-search">
            <label class="field-label" for="availability-map-search">
              Buscar fazenda, comunidade ou referência
            </label>
            <div class="availability-map-search__row">
              <input
                id="availability-map-search"
                class="field-control"
                type="search"
                autocomplete="off"
                placeholder="Ex.: fazenda tal paracatu"
                data-availability-map-search
              />
              <button
                class="availability-map-search__button"
                type="button"
                data-availability-map-search-button
              >
                Buscar
              </button>
            </div>
            <p class="availability-map-status" data-availability-map-status>
              O mapa carrega somente depois de escolher busca ou mapa.
            </p>
          </div>

          <div class="availability-map-stage">
            <div
              class="availability-map-canvas"
              role="application"
              aria-label="Mapa interativo para selecionar a localização"
              data-availability-map-canvas
            >
            </div>

            <div
              class="availability-map-fallback"
              data-availability-map-fallback
              hidden
            >
              <span class="availability-map-fallback__icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" focusable="false">
                  <path d="M12 21s6-5.4 6-10a6 6 0 1 0-12 0c0 4.6 6 10 6 10Z"
                  ></path>
                  <path d="m9.5 9.5 5 5"></path>
                  <path d="m14.5 9.5-5 5"></path>
                </svg>
              </span>
              <h3>Mapa indisponível agora</h3>
              <p data-availability-map-fallback-message>
                Use GPS, coordenadas manuais ou WhatsApp para continuar.
              </p>
              <div class="availability-map-fallback__actions">
                <button type="button" data-availability-map-retry>
                  Tentar novamente
                </button>
                <button type="button" data-availability-map-use-gps>
                  Usar GPS
                </button>
                <button type="button" data-availability-map-use-coordinates>
                  Informar coordenadas
                </button>
                <a
                  href={`https://wa.me/${site.whatsapp.digits}`}
                  target="_blank"
                  rel="noopener noreferrer"
                >
                  WhatsApp
                </a>
              </div>
            </div>
          </div>

          <div class="availability-map-selection">
            <div>
              <p class="section-kicker">Ponto selecionado</p>
              <strong data-availability-map-selected-title>
                Toque no mapa para marcar
              </strong>
              <span data-availability-map-selected-coordinates>
                Nenhum ponto confirmado ainda.
              </span>
            </div>
            <button
              class="availability-map-confirm"
              type="button"
              data-availability-map-confirm
              disabled
            >
              <svg aria-hidden="true" focusable="false" viewBox="0 0 24 24">
                <path d="M12 21s6-5.4 6-10a6 6 0 1 0-12 0c0 4.6 6 10 6 10Z"
                ></path>
                <circle cx="12" cy="11" r="2"></circle>
              </svg>
              Confirmar este ponto
            </button>
          </div>
        </aside>
      </div>
    </div>
  </dialog>
</section>

<script>
  import "../../scripts/availability";
</script>

<style>
  .availability-modal-host {
    display: contents;
  }

  .availability-modal {
    width: 100%;
    max-width: none;
    height: 100dvh;
    max-height: none;
    margin: 0;
    border: 0;
    background: transparent;
    color: var(--color-text-primary);
    padding: 0;
    z-index: 100;
  }

  .availability-modal::backdrop {
    background: rgba(0, 0, 0, 0.74);
    backdrop-filter: blur(3px);
  }

  .availability-modal__surface {
    position: relative;
    display: grid;
    height: 100%;
    grid-template-rows: auto auto minmax(0, 1fr);
    overflow: hidden;
    border: 1px solid rgba(161, 242, 124, 0.16);
    background:
      radial-gradient(
        circle at 8% 8%,
        rgba(99, 212, 59, 0.18),
        transparent 16rem
      ),
      linear-gradient(145deg, rgba(6, 21, 20, 0.98), rgba(1, 7, 8, 0.98));
    box-shadow: 0 1.5rem 4rem rgba(0, 0, 0, 0.48);
  }

  .availability-modal__header {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 0.8rem;
    align-items: start;
    padding: max(0.85rem, env(safe-area-inset-top)) 0.85rem 0.65rem;
  }

  .availability-modal__title-group {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 0.75rem;
    align-items: center;
    min-width: 0;
  }

  .availability-modal__target {
    display: grid;
    width: 2.7rem;
    height: 2.7rem;
    flex: 0 0 auto;
    place-items: center;
    border-radius: 999px;
    background: rgba(99, 212, 59, 0.12);
    color: var(--color-green-300);
  }

  .availability-modal__target svg,
  .availability-modal__close svg,
  .availability-region-card svg,
  .availability-realtime svg,
  .availability-action svg,
  .availability-submit svg {
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .availability-modal__target svg {
    width: 2.25rem;
    height: 2.25rem;
    stroke-width: 2.2;
  }

  .availability-modal__header h2 {
    font-size: clamp(1.35rem, 6vw, 2rem);
    line-height: 1.05;
    margin: 0;
  }

  .availability-modal__header p {
    color: var(--color-text-secondary);
    font-size: 0.86rem;
    line-height: 1.35;
    margin: 0.25rem 0 0;
  }

  .availability-modal__close {
    display: grid;
    width: 3rem;
    height: 3rem;
    place-items: center;
    border: 1px solid var(--color-border-subtle);
    border-radius: var(--radius-control);
    background: rgba(245, 247, 243, 0.04);
    color: var(--color-text-primary);
    cursor: pointer;
  }

  .availability-modal__close svg {
    width: 1.15rem;
    height: 1.15rem;
    stroke-width: 2.4;
  }

  .availability-modal__stepper {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.35rem;
    margin: 0;
    padding: 0 0.85rem 0.65rem;
    list-style: none;
  }

  .availability-modal__stepper li {
    position: relative;
    display: grid;
    justify-items: center;
    gap: 0.28rem;
    color: var(--color-text-muted);
    font-size: 0.68rem;
    font-weight: 840;
    line-height: 1.1;
    text-align: center;
  }

  .availability-modal__stepper li:not(:last-child)::after {
    position: absolute;
    top: 0.88rem;
    left: calc(50% + 1.05rem);
    width: calc(100% - 1.65rem);
    height: 1px;
    background: rgba(245, 247, 243, 0.18);
    content: "";
  }

  .availability-modal__stepper span {
    position: relative;
    z-index: 1;
    display: grid;
    width: 1.75rem;
    height: 1.75rem;
    place-items: center;
    border: 1px solid rgba(245, 247, 243, 0.22);
    border-radius: 999px;
    background: rgba(2, 11, 12, 0.8);
    color: var(--color-text-secondary);
    font-size: 0.78rem;
  }

  .availability-modal__stepper .is-active span {
    border-color: var(--color-green-300);
    color: var(--color-green-300);
    box-shadow: 0 0 0 0.25rem rgba(161, 242, 124, 0.1);
  }

  .availability-modal__body {
    display: grid;
    gap: 0.8rem;
    min-height: 0;
    overflow-y: auto;
    padding: 0 0.85rem max(0.85rem, env(safe-area-inset-bottom));
  }

  .availability-modal__controls {
    display: grid;
    gap: 0.7rem;
    min-width: 0;
  }

  .availability-region-card,
  .availability-coordinate-card,
  .availability-modal__map,
  .availability-map-search {
    display: grid;
    gap: 0.5rem;
    padding: 0.75rem;
  }

  .availability-coordinate-card[hidden],
  .availability-modal__map[hidden] {
    display: none;
  }

  .availability-map-search__row {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 0.45rem;
    align-items: stretch;
  }

  .availability-map-search__button,
  .availability-map-confirm,
  .availability-map-fallback__actions button,
  .availability-map-fallback__actions a {
    min-height: var(--touch-target-min);
    border-radius: var(--radius-control);
    font-weight: 900;
  }

  .availability-map-search__button {
    border: 1px solid var(--color-primary-hover);
    background: var(--gradient-primary);
    color: var(--color-text-inverse);
    cursor: pointer;
    padding-inline: 0.85rem;
  }

  .availability-map-search__button:disabled {
    border-color: var(--color-border-subtle);
    background: rgba(245, 247, 243, 0.08);
    color: var(--color-text-muted);
    cursor: progress;
  }

  .availability-map-status {
    color: var(--color-text-muted);
    font-size: 0.8rem;
    line-height: 1.35;
    margin: 0;
  }

  .availability-map-stage {
    position: relative;
    min-height: 16rem;
    overflow: hidden;
    border-block: 1px solid var(--color-border-subtle);
    background:
      linear-gradient(90deg, rgba(245, 247, 243, 0.045) 1px, transparent 1px),
      linear-gradient(0deg, rgba(245, 247, 243, 0.045) 1px, transparent 1px),
      radial-gradient(
        circle at 50% 52%,
        rgba(99, 212, 59, 0.22),
        transparent 5rem
      ),
      linear-gradient(145deg, rgba(5, 25, 22, 0.98), rgba(2, 11, 12, 0.94));
    background-size:
      2rem 2rem,
      2rem 2rem,
      auto,
      auto;
  }

  .availability-map-canvas {
    width: 100%;
    min-height: 16rem;
    height: 100%;
  }

  .availability-map-canvas:empty::before {
    position: absolute;
    inset: 0;
    display: grid;
    place-items: center;
    color: var(--color-text-muted);
    font-size: 0.9rem;
    padding: 1rem;
    text-align: center;
    content: "Carregando mapa...";
  }

  .availability-map-fallback {
    position: absolute;
    inset: 0;
    z-index: 2;
    display: grid;
    align-content: center;
    justify-items: center;
    gap: 0.65rem;
    padding: 1rem;
    background: rgba(2, 11, 12, 0.9);
    text-align: center;
  }

  .availability-map-fallback[hidden] {
    display: none;
  }

  .availability-map-fallback__icon {
    display: grid;
    width: 3.2rem;
    height: 3.2rem;
    place-items: center;
    border: 1px solid rgba(241, 120, 107, 0.48);
    border-radius: 999px;
    background: rgba(241, 120, 107, 0.1);
    color: var(--color-error);
  }

  .availability-map-fallback__icon svg,
  .availability-map-confirm svg {
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .availability-map-fallback__icon svg {
    width: 1.75rem;
    height: 1.75rem;
    stroke-width: 2.1;
  }

  .availability-map-fallback h3 {
    font-size: 1.15rem;
    line-height: 1.15;
    margin: 0;
  }

  .availability-map-fallback p {
    max-width: 25rem;
    color: var(--color-text-secondary);
    font-size: 0.88rem;
    line-height: 1.42;
    margin: 0;
  }

  .availability-map-fallback__actions {
    display: grid;
    width: min(100%, 24rem);
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.45rem;
  }

  .availability-map-fallback__actions button,
  .availability-map-fallback__actions a {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border: 1px solid var(--color-border-subtle);
    background: rgba(245, 247, 243, 0.05);
    color: var(--color-text-primary);
    cursor: pointer;
    font-size: 0.84rem;
    padding: 0.68rem 0.7rem;
    text-align: center;
    text-decoration: none;
  }

  .availability-map-fallback__actions a {
    border-color: var(--color-border-green);
    color: var(--color-green-300);
  }

  .availability-map-selection {
    display: grid;
    gap: 0.65rem;
    padding: 0.75rem;
  }

  .availability-map-selection strong,
  .availability-map-selection span {
    display: block;
  }

  .availability-map-selection strong {
    font-size: 1rem;
    line-height: 1.2;
  }

  .availability-map-selection span {
    color: var(--color-text-muted);
    font-size: 0.84rem;
    line-height: 1.35;
    margin-top: 0.16rem;
  }

  .availability-map-confirm {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.55rem;
    border: 1px solid var(--color-primary-hover);
    background: var(--gradient-primary);
    color: var(--color-text-inverse);
    cursor: pointer;
    padding: 0.8rem 0.95rem;
  }

  .availability-map-confirm:disabled {
    border-color: var(--color-border-subtle);
    background: rgba(245, 247, 243, 0.08);
    color: var(--color-text-muted);
    cursor: not-allowed;
  }

  .availability-map-confirm svg {
    width: 1.12rem;
    height: 1.12rem;
    stroke-width: 2.2;
  }

  .availability-result {
    border: 1px solid var(--color-border-subtle);
    border-radius: var(--radius-card);
    background: rgba(2, 11, 12, 0.34);
  }

  .availability-region-card {
    display: grid;
    gap: 0.55rem;
    padding: 0.75rem;
  }

  .availability-region-card__copy {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 0.55rem;
    align-items: center;
  }

  .availability-region-card__copy svg {
    width: 2.05rem;
    height: 2.05rem;
    color: var(--color-green-300);
    stroke-width: 2.1;
  }

  .availability-region-card h3 {
    font-size: 1rem;
    line-height: 1.15;
    margin: 0;
  }

  .availability-region-card p,
  .availability-modal__microcopy {
    color: var(--color-text-muted);
    font-size: 0.82rem;
    line-height: 1.32;
    margin: 0;
  }

  .availability-region-card .field-control,
  .availability-coordinate-card .field-control {
    min-height: 2.85rem;
    font-size: 1rem;
    padding-block: 0.72rem;
  }

  .availability-realtime {
    display: grid;
    min-height: 5.75rem;
    grid-template-columns: auto minmax(0, 1fr) auto;
    gap: 0.75rem;
    align-items: center;
    border: 1px solid var(--color-primary-hover);
    border-radius: 1rem;
    background: var(--gradient-primary);
    color: var(--color-text-inverse);
    cursor: pointer;
    font-size: 1.06rem;
    font-weight: 950;
    line-height: 1.15;
    padding: 0.85rem;
    text-align: left;
    box-shadow: 0 0 0 0.35rem rgba(99, 212, 59, 0.08);
  }

  .availability-realtime:disabled {
    cursor: progress;
    opacity: 0.78;
  }

  .availability-realtime__icon {
    display: grid;
    width: 2.95rem;
    height: 2.95rem;
    place-items: center;
    border: 2px solid rgba(4, 17, 13, 0.32);
    border-radius: 999px;
  }

  .availability-realtime svg {
    width: 2rem;
    height: 2rem;
    stroke-width: 2.4;
  }

  .availability-realtime small {
    border-radius: var(--radius-chip);
    background: rgba(4, 17, 13, 0.26);
    font-size: 0.72rem;
    font-weight: 950;
    padding: 0.35rem 0.58rem;
  }

  .availability-modal__divider {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    gap: 0.55rem;
    align-items: center;
    color: var(--color-text-muted);
    font-size: 0.68rem;
    font-weight: 950;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .availability-modal__divider span {
    height: 1px;
    background: rgba(245, 247, 243, 0.16);
  }

  .availability-actions {
    display: grid;
    gap: 0.45rem;
  }

  .availability-action {
    display: grid;
    width: 100%;
    min-height: 3.6rem;
    grid-template-columns: auto minmax(0, 1fr) auto;
    gap: 0.65rem;
    align-items: center;
    border: 1px solid var(--color-border-subtle);
    border-radius: var(--radius-control);
    background: rgba(245, 247, 243, 0.035);
    color: var(--color-text-primary);
    cursor: pointer;
    padding: 0.6rem;
    text-align: left;
  }

  .availability-action:hover,
  .availability-action:focus-visible {
    border-color: var(--color-border-green);
    background: rgba(99, 212, 59, 0.08);
  }

  .availability-action__icon {
    display: grid;
    width: 2.45rem;
    height: 2.45rem;
    place-items: center;
    border: 1px solid var(--color-border-green);
    border-radius: 0.72rem;
    color: var(--color-green-300);
  }

  .availability-action__icon svg {
    width: 1.28rem;
    height: 1.28rem;
    stroke-width: 2.3;
  }

  .availability-action strong,
  .availability-action small {
    display: block;
  }

  .availability-action strong {
    font-size: 0.92rem;
    line-height: 1.15;
  }

  .availability-action small {
    color: var(--color-text-muted);
    font-size: 0.76rem;
    line-height: 1.25;
    margin-top: 0.1rem;
  }

  .availability-action__arrow {
    width: 1.15rem;
    height: 1.15rem;
    color: var(--color-text-muted);
    stroke-width: 2.3;
  }

  .availability-coordinate-card {
    display: grid;
    gap: 0.55rem;
    padding: 0.75rem;
  }

  .availability-coordinate-card__heading {
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
    align-items: center;
    justify-content: space-between;
  }

  .availability-coordinate-card__heading span {
    border: 1px solid var(--color-border-green);
    border-radius: var(--radius-chip);
    background: var(--color-green-soft);
    color: var(--color-green-300);
    font-size: 0.72rem;
    font-weight: 900;
    line-height: 1;
    padding: 0.42rem 0.58rem;
  }

  .field-hint,
  .availability-modal__helper {
    color: var(--color-text-muted);
    font-size: 0.82rem;
    line-height: 1.35;
    margin: 0;
  }

  .availability-modal__helper {
    border: 1px solid var(--color-border-subtle);
    border-radius: var(--radius-control);
    background: rgba(245, 247, 243, 0.035);
    padding: 0.65rem 0.75rem;
  }

  .availability-submit {
    display: inline-flex;
    min-height: 3.35rem;
    align-items: center;
    justify-content: center;
    gap: 0.7rem;
    border: 1px solid var(--color-primary-hover);
    border-radius: 1rem;
    background: var(--gradient-primary);
    color: var(--color-text-inverse);
    cursor: pointer;
    font-weight: 950;
    padding: 0.9rem 1rem;
  }

  .availability-submit:disabled {
    border-color: var(--color-border-subtle);
    background: rgba(245, 247, 243, 0.08);
    color: var(--color-text-muted);
    cursor: not-allowed;
  }

  .availability-submit svg {
    width: 1.2rem;
    height: 1.2rem;
    stroke-width: 2.3;
  }

  .availability-modal__privacy {
    margin: 0;
  }

  .availability-modal__map {
    position: relative;
    display: grid;
    min-height: 14rem;
    overflow: hidden;
    padding: 0;
  }

  .availability-result {
    display: grid;
    gap: 0.65rem;
    margin: 0;
    padding: 0.9rem;
    box-shadow: 0 1rem 2rem rgba(0, 0, 0, 0.28);
  }

  .availability-result[hidden] {
    display: none;
  }

  .availability-result__eyebrow {
    color: var(--color-green-300);
    font-size: 0.72rem;
    font-weight: 950;
    letter-spacing: 0.08em;
    line-height: 1.15;
    margin: 0;
    text-transform: uppercase;
  }

  .availability-result h3 {
    font-size: 1.3rem;
    line-height: 1.14;
    margin: 0;
  }

  .availability-result p:not(.availability-result__eyebrow) {
    color: var(--color-text-secondary);
    font-size: 0.9rem;
    line-height: 1.42;
    margin: 0;
  }

  .availability-result__actions {
    display: grid;
    gap: 0.5rem;
  }

  .availability-result__whatsapp,
  .availability-result__edit {
    display: inline-flex;
    min-height: var(--touch-target-min);
    align-items: center;
    justify-content: center;
    border-radius: var(--radius-control);
    font-weight: 900;
    padding: 0.78rem 0.9rem;
    text-align: center;
    text-decoration: none;
  }

  .availability-result__whatsapp {
    border: 1px solid var(--color-border-green);
    color: var(--color-green-300);
  }

  .availability-result__edit {
    border: 1px solid var(--color-border-subtle);
    background: rgba(245, 247, 243, 0.04);
    color: var(--color-text-secondary);
    cursor: pointer;
  }

  .availability-result[data-status="pre_viavel"] {
    border-color: var(--color-border-green);
  }

  .availability-result[data-status="sem_viabilidade_automatica"],
  .availability-result[data-status="necessita_confirmacao"] {
    border-color: rgba(216, 155, 95, 0.5);
  }

  .availability-result[data-status="entrada_invalida"],
  .availability-result[data-status="limite_excedido"],
  .availability-result[data-status="erro_temporario"],
  .availability-result[data-status="falha_rede"] {
    border-color: rgba(241, 120, 107, 0.58);
  }

  :global(body.availability-modal-is-open) {
    overflow: hidden;
  }

  @media (max-width: 57.99rem) {
    .availability-modal__header {
      padding-block: max(0.6rem, env(safe-area-inset-top)) 0.45rem;
    }

    .availability-modal__target {
      width: 2.35rem;
      height: 2.35rem;
    }

    .availability-modal__target svg {
      width: 1.95rem;
      height: 1.95rem;
    }

    .availability-modal__header h2 {
      font-size: 1.28rem;
    }

    .availability-modal__header p {
      font-size: 0.78rem;
      line-height: 1.25;
    }

    .availability-modal__stepper {
      padding-bottom: 0.45rem;
    }

    .availability-modal__stepper li {
      font-size: 0.62rem;
    }

    .availability-modal__stepper span {
      width: 1.55rem;
      height: 1.55rem;
    }

    .availability-modal__stepper li:not(:last-child)::after {
      top: 0.78rem;
    }

    .availability-modal__body,
    .availability-modal__controls {
      gap: 0.5rem;
    }

    .availability-region-card,
    .availability-coordinate-card {
      gap: 0.4rem;
      padding: 0.55rem;
    }

    .availability-region-card__copy {
      gap: 0.45rem;
    }

    .availability-region-card__copy p,
    .availability-action small,
    .availability-modal__privacy {
      display: none;
    }

    .availability-region-card__copy svg {
      width: 1.75rem;
      height: 1.75rem;
    }

    .availability-region-card h3 {
      font-size: 0.92rem;
    }

    .availability-region-card .field-control,
    .availability-coordinate-card .field-control {
      min-height: 2.55rem;
      padding-block: 0.6rem;
    }

    .availability-realtime {
      min-height: 4.35rem;
      gap: 0.6rem;
      padding: 0.65rem;
    }

    .availability-realtime__icon {
      width: 2.45rem;
      height: 2.45rem;
    }

    .availability-realtime svg {
      width: 1.65rem;
      height: 1.65rem;
    }

    .availability-action {
      min-height: 3rem;
      padding: 0.45rem;
    }

    .availability-action__icon {
      width: 2.1rem;
      height: 2.1rem;
    }

    .availability-modal__helper {
      padding: 0.52rem 0.65rem;
    }

    .availability-submit {
      min-height: 3.1rem;
      padding-block: 0.75rem;
    }
  }
  @media (max-width: 24rem) {
    .availability-action small {
      display: none;
    }
  }

  @media (min-width: 58rem) {
    .availability-modal {
      width: min(100% - 3rem, 92rem);
      height: min(100dvh - 3rem, 56rem);
      margin: auto;
      border-radius: 1.25rem;
    }

    .availability-modal__surface {
      border-radius: 1.25rem;
    }

    .availability-modal__header {
      padding: 1.35rem 1.55rem 0.9rem;
    }

    .availability-modal__target {
      width: 3.4rem;
      height: 3.4rem;
    }

    .availability-modal__target svg {
      width: 2.75rem;
      height: 2.75rem;
    }

    .availability-modal__stepper {
      max-width: 36rem;
      margin-inline: auto 2rem;
      padding-bottom: 1rem;
    }

    .availability-modal__stepper li {
      font-size: 0.78rem;
    }

    .availability-modal__stepper span {
      width: 2rem;
      height: 2rem;
    }

    .availability-modal__body {
      grid-template-columns: minmax(22rem, 0.86fr) minmax(30rem, 1.14fr);
      align-items: stretch;
      overflow: hidden;
      padding: 0 1.35rem 1.35rem;
    }

    .availability-modal__body:has(.availability-modal__map[hidden]) {
      grid-template-columns: minmax(0, 42rem);
      justify-content: center;
    }

    .availability-modal__controls,
    .availability-modal__map {
      min-height: 0;
    }

    .availability-modal__controls {
      align-content: start;
      overflow-y: auto;
      border: 1px solid var(--color-border-subtle);
      border-radius: var(--radius-card-lg);
      padding: 1rem;
    }

    .availability-modal__map {
      min-height: 0;
    }

    .availability-result {
      padding: 1rem;
      background: rgba(3, 16, 15, 0.72);
    }

    .availability-result__actions {
      grid-template-columns: minmax(0, 1fr) auto;
      align-items: center;
    }
  }

  @media (min-width: 72rem) {
    .availability-modal__body {
      grid-template-columns: minmax(28rem, 0.82fr) minmax(40rem, 1.18fr);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .availability-action,
    .availability-realtime,
    .availability-submit,
    .availability-modal__close {
      transition: none;
    }
  }
</style>
